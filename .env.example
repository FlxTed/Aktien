# Database (PostgreSQL required - use Neon, Vercel Postgres, or any Postgres host)
# Local: use Neon free tier or Docker. Vercel: set this in project env.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Stock API (Finnhub - free tier: 60 calls/min)
FINNHUB_API_KEY="your-finnhub-api-key"

# OpenAI for AI analysis
OPENAI_API_KEY="sk-your-openai-api-key"

# Email (Resend recommended for alerts)
RESEND_API_KEY="re_your-resend-api-key"
EMAIL_FROM="alerts@yourdomain.com"

# Vercel Cron (set in Vercel project env; protects /api/cron/check-alerts)
# CRON_SECRET=your-secret

# Optional: Google OAuth
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
